package com.instantdessert.stockapp.data.db.entity

enum class Unite { G, KG, L, ML, UNITE }

enum class Canal { RESTAURANT, PARTICULIER }

enum class TypeMouvement { ACHAT_MP, CORRECTION_MP, PRODUCTION, VENTE }

enum class EntiteType { MATIERE_PREMIERE, PRODUIT_FINI }
